apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "client-registry.fullname" . }}-test-connection"
  labels:
{{ include "client-registry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "client-registry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
